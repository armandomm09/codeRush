#include <iostream>
#include <vector>
#include <math.h>

using namespace std;

int main()
{
    int q, n;
    cin >> q;

    int max_number = 0;
    int numbers = 0;

    cin >> n;

    for (int i = 0; i < n; i++)
    {
        max_number += 9 * pow(10, i);
    }

    for (int k = 0; k < max_number)

    return 0;
}